
import { BaseModel } from '../BaseModel';

export class CustomerUserModel extends BaseModel {
    static resource = 'customer_user';

    constructor(properties) {
        super(properties);
    }
}
