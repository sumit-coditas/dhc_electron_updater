import { BaseModel } from ".././BaseModel";

export class ReferenceGridScopesModel extends BaseModel {
    static resource = 'reference_grid_scopes';

    constructor(properties) {
        super(properties);
    }
}