import { BaseModel } from '../BaseModel';

export class DrafterAdminModal extends BaseModel {
    static resource = 'drafter_admin_data';
    constructor(properties) {
        super(properties);
    }
}
